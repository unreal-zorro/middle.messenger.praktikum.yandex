<input
  class="input{{#if className}} {{ className }}{{/if}}"
  type={{ type }}
  name={{ name }}
  value={{ value }}
  placeholder={{ placeholder }}
  {{#if disabled}} {{ disabled }}{{/if}}
>
</input>
