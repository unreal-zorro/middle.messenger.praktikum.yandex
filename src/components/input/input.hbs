<input
  class="input{{#if className}} {{ className }}{{/if}}"
  type={{ type }}
  name={{ name }}
  placeholder={{ placeholder }}
  value={{ value }}
  {{#if disabled}} {{ disabled }}{{/if}}
>
</input>
