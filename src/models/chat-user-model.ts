export interface ChatUserModel extends Record<string, number | string> {
  id: number;
  first_name: string;
  second_name: string;
  display_name: string;
  login: string;
  avatar: string;
  role: string;
}
